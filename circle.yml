machine:
    timezone: Asia/Tokyo
    ruby:
      version: 2.2.3

dependencies:
  override:
    - gem install minitest minitest-reporters toml thor
    - rake build
    - gem install --local pkg/ygl-0.1.0.gem
    - ygl --help
    - mkdir $HOME/.yet_another_glean
    - cd $HOME/.yet_another_glean
    - git clone git@github.com:bm-sms/glean-daimon-lunch.git