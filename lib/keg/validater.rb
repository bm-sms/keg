require 'keg'

module Keg
  class Validater
    def inilitalize(root)
      @root = root
      @dataaba
    end

    def self.switch(db_name)
      path = File.join()
        abort "Error: No such directroy `#{db_name}`. Please enter a correct DB name."
      end
    end

    def no_such_file
      abort "Error: No such file `#{filename}`. Please enter a correct file name."
    end
  end
end